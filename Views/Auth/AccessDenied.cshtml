@{
    ViewData["Title"] = "Erişim Reddedildi";
    Layout = "_Layout";
}

<div class="container-fluid mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 text-center">
            <div class="card border-0 shadow-sm">
                <div class="card-body p-5">
                    <i class="ti ti-shield-lock text-danger" style="font-size: 5rem;"></i>
                    <h2 class="mt-4 mb-3">Erişim Reddedildi</h2>
                    <p class="text-secondary mb-4">Bu sayfayı görüntülemek veya bu işlemi yapmak için yetkiniz bulunmamaktadır.</p>
                    <a href="~/" class="btn btn-primary px-4">
                        <i class="ti ti-home me-2"></i>Ana Sayfaya Dön
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
